import { NextResponse } from "next/server";
import { createClient as createServiceClient } from "@supabase/supabase-js";
import { createClient as createServerClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export async function GET(){
 const auth=await createServerClient();const{data:{user}}=await auth.auth.getUser();if(!user)return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});
 const ownerId=(process.env.TA14_SEO_OWNER_USER_ID||process.env.TA14_REVENUE_OWNER_USER_ID||"").trim();const adminEmails=new Set((process.env.TA14_SEO_ADMIN_EMAILS||process.env.NEXT_PUBLIC_TA14_MISSION_CONTROL_ADMIN_EMAILS||"ta14admissibleexecution@gmail.com,greggbutlerac@gmail.com").split(",").map(v=>v.trim().toLowerCase()).filter(Boolean));const email=(user.email||"").trim().toLowerCase();const allowed=(ownerId&&user.id===ownerId)||(email&&adminEmails.has(email));if(!allowed)return NextResponse.json({error:"OWNER_ACCESS_REQUIRED"},{status:403});
 const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return NextResponse.json({error:"NOT_CONFIGURED"},{status:503});
 const db=createServiceClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const since=new Date(Date.now()-30*86400000).toISOString();
 const[eventsResult,bindingsResult,subsResult]=await Promise.all([
  db.from("ta14_seo_intelligence_events").select("occurred_at,event_type,visit_id,page_path,target_text,referrer_host,search_engine,search_query,utm_source,utm_campaign,utm_term,country,region,city,device_class,intent_type,intent_score").gte("occurred_at",since).order("occurred_at",{ascending:false}).limit(15000),
  db.from("ta14_seo_user_attribution").select("user_id,first_visit_id,latest_visit_id,first_seen_at,latest_seen_at,first_touch,latest_touch").order("latest_seen_at",{ascending:false}).limit(5000),
  db.from("ta14_seo_subscription_attribution_v1").select("paypal_subscription_id,owner_user_id,plan_key,status,status_changed_at,first_visit_id,latest_visit_id,first_touch,latest_touch").order("status_changed_at",{ascending:false}).limit(5000)
 ]);
 if(eventsResult.error)return NextResponse.json({error:"SEO_TELEMETRY_QUERY_FAILED"},{status:500});
 const rows=eventsResult.data||[],bindings=bindingsResult.error?[]:(bindingsResult.data||[]),subscriptions=subsResult.error?[]:(subsResult.data||[]),views=rows.filter((r:any)=>r.event_type==="page_view"),clicks=rows.filter((r:any)=>r.event_type==="click"),intents=rows.filter((r:any)=>r.event_type==="commercial_intent");
 function rank(key:string,source:any[]=views){const counts:Record<string,number>={};for(const r of source){const v=String(r[key]||"").trim();if(v)counts[v]=(counts[v]||0)+1;}return Object.entries(counts).map(([name,count])=>({name,count})).sort((a,b)=>b.count-a.count).slice(0,50)}
 const loc:Record<string,number>={};for(const r of views){const v=[r.city,r.region,r.country].filter(Boolean).join(", ");if(v)loc[v]=(loc[v]||0)+1;}
 const intentVisitors=new Set(intents.map((r:any)=>r.visit_id).filter(Boolean)).size;const avgIntent=intents.length?Math.round(intents.reduce((s:number,r:any)=>s+Number(r.intent_score||0),0)/intents.length):0;
 const paidStatuses=new Set(["ACTIVE","APPROVAL_PENDING","APPROVED"]),paid=subscriptions.filter((s:any)=>paidStatuses.has(String(s.status||"").toUpperCase()));const attributedPaid=paid.filter((s:any)=>s.first_visit_id||s.latest_visit_id);const planCounts:Record<string,number>={};for(const s of paid){const p=String(s.plan_key||"Unknown");planCounts[p]=(planCounts[p]||0)+1;}const subscriptionPlans=Object.entries(planCounts).map(([name,count])=>({name,count})).sort((a,b)=>b.count-a.count);
 const degraded={identityAttribution:!!bindingsResult.error,subscriptionAttribution:!!subsResult.error};
 return NextResponse.json({generatedAt:new Date().toISOString(),windowDays:30,degraded,summary:{pageViews:views.length,uniqueVisitors:new Set(views.map((r:any)=>r.visit_id).filter(Boolean)).size,clicks:clicks.length,searchArrivals:views.filter((r:any)=>r.search_engine).length,commercialIntentEvents:intents.length,commercialIntentVisitors:intentVisitors,averageIntentScore:avgIntent,authenticatedAttributedUsers:bindings.length,paidSubscriptions:paid.length,attributedPaidSubscriptions:attributedPaid.length,attributedRevenue:0},pages:rank("page_path"),searchEngines:rank("search_engine"),searchQueries:rank("search_query"),referrers:rank("referrer_host"),campaigns:rank("utm_campaign"),sources:rank("utm_source"),terms:rank("utm_term"),devices:rank("device_class"),locations:Object.entries(loc).map(([name,count])=>({name,count})).sort((a,b)=>b.count-a.count).slice(0,50),clickTargets:rank("target_text",clicks),intentTypes:rank("intent_type",intents),intentPages:rank("page_path",intents),subscriptionPlans,attribution:{authenticatedUsers:bindings.slice(0,100),subscriptions:subscriptions.slice(0,100)},recent:rows.slice(0,100)});
}

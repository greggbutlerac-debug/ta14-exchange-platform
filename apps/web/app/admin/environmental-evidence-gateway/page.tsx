'use client';
import { useEffect, useState } from 'react';
import { buildDemoObservations, evaluateGateway, type AuthorityObject } from '@/lib/environmental-evidence-gateway';
import { summarizeEnvironmentalGatewayConformance } from '@/lib/environmental-evidence-gateway-conformance';

const policy={maxSampleAgeSeconds:60,maxGapSeconds:120,minimumContinuityWindowSeconds:600};
type Preserved={record_id:string;replay_id:string;determination:string;preserved_at:string;replay_verification:{status:'PASS'|'FAIL';recomputed_hash:string;stored_hash:string}};
export default function EnvironmentalEvidenceGatewayPage(){const[now]=useState(()=>new Date());const observations=buildDemoObservations(now);const authority:AuthorityObject={authorityId:'AUTH-VENT-001',issuer:'TA-14 PRIVATE R1 DEMONSTRATION',subject:'DECLARED OPERATOR',zoneScope:'declared-zone-01',permittedConsequence:'ventilation_increase_only',effectiveAt:new Date(now.getTime()-3600000).toISOString(),expiresAt:new Date(now.getTime()+3600000).toISOString(),revocationState:'ACTIVE',sourceReference:'PRIVATE-R1-DEMO-AUTHORITY'};const result=evaluateGateway({observations,policy,authority,consequence:'ventilation_increase_only',now:now.toISOString()});const conformance=summarizeEnvironmentalGatewayConformance(now);const latest=observations.at(-1)!;const[receipts,setReceipts]=useState<Preserved[]>([]),[status,setStatus]=useState(''),[busy,setBusy]=useState(false);
 async function load(){try{const r=await fetch('/api/admin/environmental-evidence-gateway/receipts',{cache:'no-store'});const j=await r.json();if(r.ok)setReceipts(j.receipts??[]);else setStatus(j.error??'Receipt ledger unavailable.')}catch{setStatus('Receipt ledger unavailable.')}}useEffect(()=>{void load()},[]);
 async function preserve(){setBusy(true);setStatus('Verifying and preserving…');try{const r=await fetch('/api/admin/environmental-evidence-gateway/receipts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({record_id:result.recordId,replay_id:result.receipt.replayId,evidence_hash:result.receipt.evidenceHash,determination_hash:result.receipt.hash,determination:result.determination,receipt_payload:result.receipt.payload})});const j=await r.json();if(!r.ok)throw new Error(j.error??'Preservation failed.');setStatus(`PRESERVED · replay verification ${j.replay_verification.status}`);await load()}catch(e){setStatus(e instanceof Error?e.message:'Preservation failed.')}finally{setBusy(false)}}
 return <main style={{maxWidth:1180,margin:'0 auto',padding:'48px 24px 80px',fontFamily:'Arial,sans-serif',color:'#17212b'}}><div style={{fontSize:12,letterSpacing:2,fontWeight:700}}>TA-14 AUTHORITY · PRIVATE OWNER CONSOLE</div><h1 style={{fontSize:42,margin:'12px 0 4px'}}>Environmental Evidence Gateway</h1><div style={{fontSize:20,color:'#59636e'}}>HibouAir Reference Implementation · R1 private build</div><div style={{marginTop:22,border:'1px solid #9d7c35',padding:18,background:'#fffdf7'}}><strong>PRIVATE / NOT PUBLIC.</strong> Simulated HibouAir-format observations only. No HibouAir credentials, equipment execution authority, Registry publication, or public showcase.</div>
 <section style={{marginTop:34}}><h2>Live Evidence / Governed Reliance</h2><div style={{display:'grid',gridTemplateColumns:'repeat(2,minmax(0,1fr))',gap:14}}><Card title="Environmental evidence"><p>CO₂: <b>{latest.measurements.co2Ppm} ppm</b></p><p>PM2.5: <b>{latest.measurements.pm25UgM3} µg/m³</b></p><p>VOC: <b>{latest.measurements.voc}</b></p><p>Temperature: <b>{latest.measurements.temperatureC} °C</b> · RH: <b>{latest.measurements.rhPct}%</b></p><p>Source: <b>{latest.source.deviceId}</b> · Zone: <b>{latest.zone}</b></p></Card><Card title="Governed reliance"><p>R1A admissibility: <b>{result.admissibility}</b></p><p>Continuity: <b>{result.continuity}</b></p><p>Authority: <b>{result.authorityStatus}</b></p><p>Binding: <b>{result.bindingScope??'NONE'}</b></p><p style={{fontSize:28}}>R1B: <b>{result.determination}</b></p></Card></div></section>
 <section style={{marginTop:34}}><h2>Cryptographic receipt / replay</h2><table style={{width:'100%',borderCollapse:'collapse'}}><tbody><Row k="Algorithm" v={result.receipt.algorithm}/><Row k="Canonical receipt" v={result.receipt.canonicalVersion}/><Row k="Evidence SHA-256" v={result.receipt.evidenceHash}/><Row k="Determination SHA-256" v={result.receipt.hash}/><Row k="Replay ID" v={result.receipt.replayId}/></tbody></table><p style={{color:'#59636e'}}>The receipt is derived from canonicalized evidence and governed determination inputs. Identical evidence and inputs reproduce the same digest; changed evidence produces a different receipt.</p><button onClick={preserve} disabled={busy} style={{padding:'12px 18px',fontWeight:700,cursor:busy?'wait':'pointer'}}>Preserve verified private receipt</button>{status&&<p><b>{status}</b></p>}</section>
 <section style={{marginTop:34}}><h2>Private preserved receipts</h2>{receipts.length?<table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th style={head}>Record</th><th style={head}>Determination</th><th style={head}>Replay</th><th style={head}>Verification</th><th style={head}>Preserved</th></tr></thead><tbody>{receipts.map(r=><tr key={r.replay_id}><td style={cell}>{r.record_id}</td><td style={cell}>{r.determination}</td><td style={cell}>{r.replay_id}</td><td style={cell}><b>{r.replay_verification.status}</b></td><td style={cell}>{new Date(r.preserved_at).toLocaleString()}</td></tr>)}</tbody></table>:<p>No private receipts preserved yet.</p>}</section>
 <section style={{marginTop:34}}><h2>Preserved AIR specimen</h2><table style={{width:'100%',borderCollapse:'collapse'}}><tbody><Row k="Record" v={result.recordId}/><Row k="Proposition" v="Can the preserved environmental record support ventilation_increase_only under the declared authority?"/><Row k="Authority object" v={`${authority.authorityId} · ${authority.zoneScope} · ${authority.revocationState}`}/><Row k="Determination" v={result.determination}/><Row k="Reason codes" v={result.reasonCodes.join(' · ')}/><Row k="Valid until" v={result.validUntil??'No current standing'}/><Row k="Non-claims" v={result.limitations.join(' · ')}/></tbody></table></section>
 <section style={{marginTop:34}}><h2>R1 Conformance Suite · {conformance.overall}</h2><p><b>{conformance.passed}/{conformance.total}</b> bounded scenarios match their expected fail-closed determination.</p><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th style={head}>ID</th><th style={head}>Scenario</th><th style={head}>Expected</th><th style={head}>Actual</th><th style={head}>Status</th></tr></thead><tbody>{conformance.scenarios.map(s=><tr key={s.id}><td style={cell}>{s.id}</td><td style={cell}>{s.name}</td><td style={cell}>{s.expected}</td><td style={cell}>{s.actual}</td><td style={cell}><b>{s.pass?'PASS':'FAIL'}</b></td></tr>)}</tbody></table></section>
 <section style={{marginTop:34}}><h2>R1 build status</h2><p><b>Implemented:</b> canonical observation model, continuity policy, authority object, binding check, determination logic, reason codes, AIR specimen, SHA-256 canonical receipt/replay identity, owner-only Exchange surface, private 12-scenario conformance harness, and owner-scoped durable receipt preservation/replay verification.</p><p><b>Still intentionally absent:</b> live HibouAir API adapter, equipment execution, public navigation, Registry publication, or public showcase.</p><p><b>Next gate:</b> verify the durable private ledger in deployed production, then prepare the bounded live HibouAir adapter without opening any public Exchange surface.</p></section></main>}
 const head={textAlign:'left' as const,padding:10,border:'1px solid #d8dde3',background:'#17212b',color:'#fff'};const cell={padding:10,border:'1px solid #d8dde3',wordBreak:'break-word' as const};function Card({title,children}:{title:string;children:React.ReactNode}){return <div style={{border:'1px solid #d8dde3',padding:22,background:'#fff'}}><h3 style={{marginTop:0}}>{title}</h3>{children}</div>}function Row({k,v}:{k:string;v:string}){return <tr><th style={{textAlign:'left',verticalAlign:'top',width:190,padding:12,border:'1px solid #d8dde3',background:'#f5f7f9'}}>{k}</th><td style={{padding:12,border:'1px solid #d8dde3',wordBreak:'break-all'}}>{v}</td></tr>}

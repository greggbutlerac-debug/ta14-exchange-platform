'use client';

import Link from 'next/link';
import { useState } from 'react';

type Control={control:string;pass:boolean;detail:string};
type Result={schema:string;executedAt:string;counts:{total:number;passed:number;failed:number};determination:'PASS'|'FAIL';controls:Control[];boundary:string};

export default function InstitutionalAcceptancePage(){
 const[result,setResult]=useState<Result|null>(null);const[error,setError]=useState('');const[running,setRunning]=useState(false);
 async function run(){setRunning(true);setError('');setResult(null);try{const response=await fetch('/api/acceptance/institutional-finding',{method:'POST'});const body=await response.json();if(!response.ok)throw new Error(body.error||'Acceptance execution failed.');setResult(body as Result)}catch(e){setError(e instanceof Error?e.message:'Acceptance execution failed.')}finally{setRunning(false)}}
 function preserve(){if(!result)return;const blob=new Blob([JSON.stringify(result,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`TA14-Institutional-Acceptance-${result.executedAt.replace(/[:.]/g,'-')}.json`;a.click();URL.revokeObjectURL(url)}
 return <main><section className="shell"><nav><Link href="/workspace/ai-governance/adversarial-examination/review">← Institutional Review</Link><b>PRODUCTION ACCEPTANCE</b></nav><header><p>CONTROLLED EXECUTION · PRESERVED EVIDENCE</p><h1>Institutional examination<br/><em>acceptance harness.</em></h1><span>This authenticated surface executes only the currently implemented acceptance controls. It does not convert a partial harness PASS into end-to-end production acceptance.</span></header><section className="panel"><h2>Execute deployed controls</h2><p>Current scope: N02, N03, P04, P05, and the cryptographic-verification portion of N04.</p><button onClick={run} disabled={running}>{running?'Executing…':'Run controlled production acceptance'}</button></section>{error&&<section className="error">{error}</section>}{result&&<><section className={`result ${result.determination==='PASS'?'pass':'fail'}`}><small>HARNESS DETERMINATION</small><h2>{result.determination}</h2><strong>{result.counts.passed}/{result.counts.total} controls passed</strong><time>{result.executedAt}</time>{result.controls.map(control=><article key={control.control}><b>{control.control}</b><strong>{control.pass?'PASS':'FAIL'}</strong><p>{control.detail}</p></article>)}<button onClick={preserve}>Preserve execution evidence JSON</button></section><section className="boundary"><strong>Representation boundary</strong><p>{result.boundary}</p><p>The overall Production Acceptance Run remains INCOMPLETE until every required positive and negative control has preserved execution evidence.</p></section></>}<style jsx>{`main{min-height:100vh;background:#02070d;color:#edf7fb}.shell{width:min(1000px,calc(100% - 32px));margin:auto;padding:24px 0 70px;font-family:Arial,sans-serif}nav{display:flex;justify-content:space-between;border:1px solid #193243;background:#071824;padding:14px 16px;border-radius:14px}a{color:#8fdcf2;text-decoration:none}header{padding:52px 0 28px}header p,small{font-size:11px;font-weight:900;letter-spacing:.14em;color:#e5b960}h1{font-size:clamp(42px,7vw,72px);line-height:.98;margin:8px 0}h1 em{font-style:normal;color:#83dff3}header span,.panel p,.boundary p{color:#9cb1bc;line-height:1.55}.panel,.result,.boundary,.error{border:1px solid #173347;background:#06141f;border-radius:16px;padding:20px;margin:14px 0}.panel button,.result button{background:#e5b960;border:0;border-radius:10px;padding:12px 15px;font-weight:900}.panel button:disabled{opacity:.55}.result{display:grid;gap:10px}.result.pass{border-color:#2e7650}.result.fail,.error{border-color:#7c3434}.result h2{font-size:42px;margin:0}.result.pass h2{color:#83e6b0}.result.fail h2,.error{color:#ff9b9b}.result time{color:#9cb1bc;font-size:12px}.result article{display:grid;grid-template-columns:70px 80px 1fr;gap:10px;align-items:start;border-top:1px solid #173347;padding-top:12px}.result article p{margin:0;color:#b8c9d1}.boundary strong{color:#e5b960}@media(max-width:650px){.result article{grid-template-columns:1fr}nav{display:grid;gap:8px}}`}</style></section></main>;
}

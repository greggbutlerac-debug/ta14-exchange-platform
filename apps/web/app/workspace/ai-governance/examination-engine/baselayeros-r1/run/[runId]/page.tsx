import { notFound } from 'next/navigation';
import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
import { appendExaminationEvent } from './actions';
import { sealExaminationRun } from './seal-actions';

export const dynamic='force-dynamic';
const panel:React.CSSProperties={border:'1px solid rgba(148,163,184,.25)',borderRadius:16,padding:20,background:'rgba(15,23,42,.58)'};const input:React.CSSProperties={width:'100%',boxSizing:'border-box',padding:10,borderRadius:8,border:'1px solid rgba(148,163,184,.35)',background:'rgba(2,6,23,.6)',color:'#e5e7eb'};
const stages=[['S0','Frozen baseline'],['S1','Initial supportable state'],['S2','Authority / evidence established'],['S3','Material condition change'],['S4','Native reassessment'],['S5','Consequential commitment challenge'],['S6','Alternate-route / bypass challenge'],['S7','Outcome and restoration evidence']] as const;
type Run={run_id:string;freeze_record_id:string;freeze_sha256:string;operator_name:string;status:string;started_at:string;final_determination:string|null;run_sha256:string|null;sealed_at:string|null;governance_registry_identifier:string|null;outcome_record:{rationale?:string}|null};type Event={id:string;event_type:string;stage_id:string|null;payload:{summary?:string;evidence_ref?:string|null};payload_sha256:string;recorded_at:string};
export default async function RunPage({params}:{params:Promise<{runId:string}>}){const{runId}=await params;const supabase=await createClient();const{data,error}=await supabase.from('consequence_examination_runs').select('run_id,freeze_record_id,freeze_sha256,operator_name,status,started_at,final_determination,run_sha256,sealed_at,governance_registry_identifier,outcome_record').eq('run_id',runId).maybeSingle();if(error||!data)notFound();const run=data as Run;const{data:eventData}=await supabase.from('consequence_examination_events').select('id,event_type,stage_id,payload,payload_sha256,recorded_at').eq('run_id',runId).order('recorded_at',{ascending:true});const events=(eventData??[])as Event[];const open=run.status==='OPEN';const action=appendExaminationEvent.bind(null,runId);const seal=sealExaminationRun.bind(null,runId);const stageSet=new Set(events.filter(e=>e.event_type==='SCENARIO_STAGE'&&e.stage_id).map(e=>e.stage_id));const ready=stages.every(([id])=>stageSet.has(id))&&events.some(e=>e.event_type==='EVIDENCE_RECEIPT')&&events.some(e=>e.event_type==='NATIVE_DETERMINATION')&&events.some(e=>e.event_type==='OUTCOME');return <main style={{maxWidth:1120,margin:'0 auto',padding:'44px 24px 80px',color:'#e5e7eb'}}>
<p style={{color:'#94a3b8',letterSpacing:'.14em',fontSize:12}}>TA-14 CONSEQUENCE EXAMINATION RUN</p><h1>BaseLayerOS R1 — S0–S7 Workspace</h1><section style={{...panel,margin:'24px 0'}}><p><strong>Run:</strong> {run.run_id}</p><p><strong>Status:</strong> {run.status}</p><p><strong>Operator:</strong> {run.operator_name}</p><p><strong>Freeze:</strong> {run.freeze_record_id}</p><p style={{overflowWrap:'anywhere'}}><strong>Freeze SHA-256:</strong> {run.freeze_sha256}</p>{run.governance_registry_identifier&&<p><strong>Registry chronology:</strong> {run.governance_registry_identifier}</p>}{run.run_sha256&&<p style={{overflowWrap:'anywhere'}}><strong>Sealed Run SHA-256:</strong> {run.run_sha256}</p>}</section>
<section><h2>Scenario chronology</h2><div style={{display:'grid',gap:12}}>{stages.map(([id,title])=>{const count=events.filter(e=>e.stage_id===id).length;return <article key={id} style={panel}><small>{id}</small><h3>{title}</h3><p>{count} preserved event(s).</p>{open&&<form action={action} style={{display:'grid',gap:8}}><input type="hidden" name="event_type" value="SCENARIO_STAGE"/><input type="hidden" name="stage_id" value={id}/><textarea name="summary" required maxLength={5000} placeholder={`Record ${id} observation`} style={input}/><input name="evidence_ref" placeholder="Evidence object / receipt reference" style={input}/><button>Append {id} Evidence</button></form>}</article>})}</div></section>
{open&&<section style={{...panel,marginTop:28}}><h2>Append non-stage evidence</h2><form action={action} style={{display:'grid',gap:8}}><select name="event_type" style={input}><option value="CHALLENGE">Adversarial challenge</option><option value="EVIDENCE_RECEIPT">Evidence receipt</option><option value="NATIVE_DETERMINATION">Native determination</option><option value="OUTCOME">Outcome</option></select><select name="stage_id" style={input}><option value="">No stage</option>{stages.map(([id])=><option key={id}>{id}</option>)}</select><textarea name="summary" required maxLength={5000} placeholder="Bounded factual record" style={input}/><input name="evidence_ref" placeholder="Evidence object / receipt reference" style={input}/><button>Append to Evidence Ledger</button></form></section>}
<section style={{marginTop:28}}><h2>Append-only evidence ledger</h2><div style={{display:'grid',gap:10}}>{events.length===0?<div style={panel}>No execution evidence recorded yet.</div>:events.map((e,i)=><article key={e.id} style={panel}><small>#{i+1} · {e.recorded_at} · {e.event_type}{e.stage_id?` · ${e.stage_id}`:''}</small><p>{e.payload?.summary}</p>{e.payload?.evidence_ref&&<p><strong>Evidence:</strong> {e.payload.evidence_ref}</p>}<p style={{overflowWrap:'anywhere',fontSize:12,color:'#94a3b8'}}>Event SHA-256: {e.payload_sha256}</p></article>)}</div></section>
{open&&<section style={{...panel,marginTop:28}}><h2>Seal examination</h2><p>{ready?'Minimum sealing evidence is present. Server-side validation will run again at commit.':'Seal remains unavailable until S0–S7, an evidence receipt, native determination, and outcome event are preserved.'}</p><form action={seal} style={{display:'grid',gap:8}}><input name="registry_identifier" disabled={!ready} placeholder="Optional registered identity, e.g. TA-14-AIGR-000031" style={input}/><small style={{color:'#94a3b8'}}>Optional linkage is validated against the public Registry before sealing. Once sealed, the linkage is part of the run digest and cannot be rewritten.</small><select name="determination" required disabled={!ready} style={input}><option value="">Select bounded finding</option><option>SUPPORTED</option><option>PARTIALLY_SUPPORTED</option><option>UNSUPPORTED</option><option>INDETERMINATE</option></select><textarea name="rationale" required disabled={!ready} maxLength={5000} placeholder="Bounded rationale tied to admitted evidence" style={input}/><button disabled={!ready}>Seal Examination Permanently</button></form></section>}
{!open&&<section style={{...panel,marginTop:28}}><h2>Sealed examination</h2><p><strong>{run.final_determination}</strong></p><p>{run.outcome_record?.rationale}</p><p>Sealed: {run.sealed_at}</p><p><Link href={`/workspace/ai-governance/examination-engine/baselayeros-r1/run/${encodeURIComponent(run.run_id)}/receipt`}>Inspect permanent receipt →</Link></p>{run.governance_registry_identifier&&<p><Link href={`/workspace/ai-governance/registry/showcase/${encodeURIComponent(run.governance_registry_identifier)}/examinations`}>Open registry examination chronology →</Link></p>}<p><strong>This record is immutable. Replay or correction requires a separately attributable successor object.</strong></p></section>}
<section style={{...panel,marginTop:28}}><h2>Execution doctrine</h2><p>Entries are append-only. Corrections become later attributable events; earlier evidence is not rewritten.</p><p><strong>Evidence must establish whether consequential progression occurred, was refused, was contained, or remained indeterminate.</strong></p></section></main>}

"use client";
import {FormEvent,useState} from "react";
import Link from "next/link";

type Determination={state:"ALLOW"|"HOLD"|"DENY"|"ESCALATE";reasonCode:string;explanation:string;evaluationTier:string};
type Candidate={id:string;providerRank:number;title:string;url:string;snippet?:string;canonicalHost?:string;provider:string;determination:Determination;deliveryRank?:number};
type RuntimeRecord={schema:string;recordId:string;request:string;profile:string;provider:string;providerLive:boolean;providerWindow?:{startRank:number;endRank:number;candidateCount:number};candidates:Candidate[];deliveryCommit:{candidateRanks:number[];deliveredCount:number;delivered:Candidate[]};notice?:string;state?:string;reasonCode?:string;explanation?:string};

export default function Page(){
 const [q,setQ]=useState("Greggory Don Butler articles");
 const [record,setRecord]=useState<RuntimeRecord|null>(null);
 const [loading,setLoading]=useState(false);
 const [failure,setFailure]=useState("");
 const rows=record?.candidates??[];
 const delivered=record?.deliveryCommit?.delivered??[];
 const count=(s:string)=>rows.filter(r=>r.determination.state===s).length;
 async function submit(e:FormEvent){e.preventDefault();const query=q.trim();if(!query)return;setLoading(true);setFailure("");setRecord(null);try{const res=await fetch("/api/asg/search",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query})});const data=await res.json();if(data?.schema)setRecord(data);else setFailure(data?.error||"ASG_RUNTIME_ERROR");}catch{setFailure("ASG_RUNTIME_UNAVAILABLE")}finally{setLoading(false)}}
 function download(){if(!record)return;const blob=new Blob([JSON.stringify(record,null,2)],{type:"application/json"});const u=URL.createObjectURL(blob);const a=document.createElement("a");a.href=u;a.download=`${record.recordId}.json`;a.click();URL.revokeObjectURL(u)}
 return <main className="page"><div className="shell"><nav><Link href="/ai-governance/admissible-computation">← ACA Showcase</Link><b>TA-14 · ADMISSIBLE SEARCH GATEWAY v0.1</b></nav>
 <header><p className="eyebrow">POST-PROVIDER · PRE-DELIVERY GOVERNANCE</p><h1>Freeze the first 10.<br/><em>Govern what crosses the boundary.</em></h1><p className="lead">A request is sent to the configured search provider. The first ten returned candidates are frozen with original provider rank, preserved as evidence, evaluated through TA-14, and only candidates with sufficient standing cross the delivery boundary. No provider result is silently replaced or manufactured.</p></header>
 <section className="flow">REQUEST → PROVIDER TOP 10 → TA-14 RECORD / CONTINUITY / ADMISSIBILITY / BINDING → ALLOW · HOLD · DENY · ESCALATE → DELIVERY COMMIT → USER</section>
 <section className="search"><form onSubmit={submit}><label>Run governed search</label><div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search request"/><button disabled={loading}>{loading?"RUNNING…":"RUN"}</button></div><p>Live provider results are used only when legitimate Google Web Search Service credentials are configured. Otherwise the request closes HOLD; ASG does not fabricate results.</p></form></section>
 {failure&&<section className="providerHold"><span>HOLD</span><h2>ASG runtime unavailable.</h2><small>{failure}</small></section>}
 {record?.state==="HOLD"&&<section className="providerHold"><span>HOLD</span><h2>No provider candidate set established.</h2><p>{record.explanation}</p><small>{record.reasonCode}</small><p className="recordline">SEARCH RECORD · {record.recordId}</p><button className="download" onClick={download}>DOWNLOAD BOUNDED RECORD (.JSON)</button></section>}
 {record&&!record.state&&<><section className="receipt"><div><small>SEARCH RECORD</small><strong>{record.recordId}</strong><p>{record.request}</p><p className="provider">{record.provider} · {record.providerLive?"LIVE PROVIDER":"NON-LIVE"}</p><button onClick={download}>DOWNLOAD BOUNDED RECORD (.JSON)</button></div><div><b>PROVIDER WINDOW {rows.length}/10</b><b>ALLOW {count("ALLOW")}</b><b>HOLD {count("HOLD")}</b><b>DENY {count("DENY")}</b><b>ESCALATE {count("ESCALATE")}</b><b>DELIVERED {delivered.length}</b></div></section>
 <section className="results"><div><p className="eyebrow">DELIVERED RESULT SET</p><h2>What crosses the boundary</h2>{delivered.length===0&&<p>No candidate acquired sufficient standing for delivery.</p>}{delivered.map(r=><article key={r.id}><strong className="rank">{r.providerRank}</strong><span className="allow">ALLOW</span><h3>{r.title}</h3><p>{r.snippet}</p><small>TA-14 · {r.determination.reasonCode} · {r.determination.evaluationTier}</small><p className="evidence">Provider rank {r.providerRank} · {r.canonicalHost||"host unestablished"}</p><a href={r.url} target="_blank" rel="noreferrer">Open admitted source →</a></article>)}</div><aside><p className="eyebrow">PRESERVED AUDIT SET</p><h2>Nothing disappears silently</h2>{rows.filter(r=>r.determination.state!=="ALLOW").map(r=><article key={r.id}><span className={r.determination.state.toLowerCase()}>{r.determination.state}</span><h3>Provider rank {r.providerRank} · {r.title}</h3><p>{r.determination.explanation}</p><small>{r.determination.reasonCode} · {r.determination.evaluationTier}</small><p className="evidence">Preserved URL · {r.url}</p></article>)}</aside></section>
 <section className="rule"><h2>ALLOW is standing for delivery — not a certificate of absolute truth.</h2><p>All provider candidates and their determinations remain in the bounded record. Original provider rank remains separate from delivered rank. Changed context requires revalidation.</p></section></>}
 </div><style jsx>{`.page{min-height:100vh;background:#02080d;color:#f5fbff;font-family:Arial,sans-serif}.shell{width:min(1180px,calc(100% - 34px));margin:auto;padding:28px 0 90px}nav{display:flex;justify-content:space-between;font-size:11px;color:#91a8b5}nav a,.results a{color:#7ee9ff}.eyebrow,small{color:#6fe7ff;font-size:10px;font-weight:900;letter-spacing:.15em}header{padding:70px 0 38px;max-width:1000px}h1,h2{font-family:Georgia,serif}h1{font-size:clamp(48px,7vw,82px);line-height:1;margin:15px 0}h1 em{color:#a8e4ef}.lead,.search p,.providerHold p,.results p,.rule p{color:#abc0ca;line-height:1.65}.flow{padding:22px;border:1px solid #6fe7ff55;border-radius:14px;color:#bceef7;font-size:12px;line-height:1.8}.search{margin-top:24px;padding:27px;background:#081a26;border:1px solid #ffffff20;border-radius:18px}.search label{display:block;font:700 25px Georgia,serif;margin-bottom:14px}.search form>div{display:grid;grid-template-columns:1fr auto;gap:10px}.search input{padding:17px;background:#02080d;border:1px solid #ffffff30;border-radius:9px;color:#fff}.search button,.receipt button,.download{padding:14px 20px;border:0;border-radius:9px;background:#9cecff;color:#06202a;font-weight:900}.search button:disabled{opacity:.55}.providerHold{margin-top:25px;padding:28px;border:1px solid #e5bd6266;border-radius:18px;background:#211b10}.providerHold>span{color:#f2d37f;font-weight:900}.recordline{font-size:11px}.receipt{display:grid;grid-template-columns:.45fr .55fr;gap:12px;padding:35px 0 12px}.receipt>div{padding:22px;border:1px solid #ffffff1d;border-radius:15px;background:#07131d}.receipt strong{display:block;font-size:20px;margin-top:8px}.receipt div:last-child{display:flex;gap:8px;flex-wrap:wrap;align-content:center}.receipt div:last-child b{padding:10px;border:1px solid #ffffff20;border-radius:8px;font-size:10px}.provider,.evidence{font-size:11px}.results{display:grid;grid-template-columns:1.1fr .9fr;gap:14px;padding:20px 0 45px}.results>div,.results>aside{padding:25px;border:1px solid #ffffff1b;border-radius:18px;background:#06111a}.results article{padding:18px 0;border-top:1px solid #ffffff14}.results h2{font-size:31px}.results h3{font-family:Georgia,serif;margin:9px 0}.results a{display:block;margin-top:12px;font-size:12px}.rank{font-size:22px;color:#6fe7ff;margin-right:10px}.allow,.hold,.deny,.escalate{display:inline-block;padding:5px 7px;border-radius:6px;font-size:9px;font-weight:900}.allow{background:#123c35;color:#8df2d7}.hold{background:#493c20;color:#f5d886}.deny{background:#4a2529;color:#ff9ca6}.escalate{background:#39294c;color:#d9b0ff}.rule{text-align:center;padding:45px 0;border-top:1px solid #ffffff16}.rule h2{font-size:clamp(30px,4vw,46px)}@media(max-width:760px){.search form>div,.receipt,.results{grid-template-columns:1fr}nav{gap:12px}}`}</style></main>
}
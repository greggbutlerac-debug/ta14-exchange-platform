"use server";

import { headers } from "next/headers";
import { redirect } from "next/navigation";

import { createClient } from "../../lib/supabase/server";

const DEFAULT_POST_AUTH_PATH = "/workspace/ai-governance/registry";
type NoticeType = "error" | "message";
type SupabaseAuthFailure = { code?: string; message?: string; status?: number };

function getRequiredField(formData: FormData, fieldName: string): string { const value=formData.get(fieldName); if(typeof value!=="string"||value.trim().length===0) throw new Error(`${fieldName} is required.`); return value.trim(); }
function safeNext(value: FormDataEntryValue | string | null | undefined): string { if(typeof value!=="string") return DEFAULT_POST_AUTH_PATH; const v=value.trim(); if(!v.startsWith("/")||v.startsWith("//")||v.startsWith("/login")||v.includes("\n")||v.includes("\r")) return DEFAULT_POST_AUTH_PATH; return v; }
async function resolveNext(formData: FormData): Promise<string> { const explicit=formData.get("next"); if(typeof explicit==="string"&&explicit.trim()) return safeNext(explicit); const h=await headers(); const referer=h.get("referer"); if(!referer) return DEFAULT_POST_AUTH_PATH; try { const u=new URL(referer); return safeNext(u.searchParams.get("next")); } catch { return DEFAULT_POST_AUTH_PATH; } }
function redirectToLogin(messageType:NoticeType,message:string,next?:string):never { const q=new URLSearchParams({[messageType]:message}); if(next&&next!==DEFAULT_POST_AUTH_PATH)q.set("next",safeNext(next)); redirect(`/login?${q.toString()}`); }
async function getRequestOrigin():Promise<string>{const h=await headers(),host=h.get("x-forwarded-host")??h.get("host"),proto=h.get("x-forwarded-proto");if(host)return `${proto??(host.startsWith("localhost")||host.startsWith("127.0.0.1")?"http":"https")}://${host}`;const configured=process.env.NEXT_PUBLIC_SITE_URL?.trim();return configured?configured.replace(/\/$/,""):"https://www.ta14authority.org";}
function normalizeAuthFailure(error:unknown):SupabaseAuthFailure{if(!error||typeof error!=="object")return{};const c=error as Record<string,unknown>;return{code:typeof c.code==="string"?c.code:undefined,message:typeof c.message==="string"?c.message:undefined,status:typeof c.status==="number"?c.status:undefined};}
function getSignupErrorMessage(error:unknown):string{const f=normalizeAuthFailure(error),code=f.code?.toLowerCase()??"",message=f.message?.toLowerCase()??"";if(code.includes("email_rate_limit")||message.includes("email rate limit")||message.includes("rate limit"))return"Account creation is temporarily waiting on the confirmation-email service. Please wait a few minutes and try again. TA-14 has recorded the service condition for review.";if(code.includes("signup_disabled")||message.includes("signups not allowed")||message.includes("signup is disabled"))return"New account creation is temporarily disabled in the Exchange authentication service. TA-14 must enable email signups before registration can continue.";if(code.includes("email_address_invalid")||message.includes("invalid email")||(message.includes("email address")&&message.includes("invalid")))return"Enter a valid email address and try again.";if(code.includes("weak_password")||(message.includes("password")&&(message.includes("weak")||message.includes("characters"))))return"Use a stronger password containing at least eight characters.";if(code.includes("user_already_exists")||message.includes("already registered")||message.includes("already exists"))return"An Exchange account already exists for this email address. Use Sign in, or use the password-recovery process if needed.";return"The Exchange authentication service did not accept the account request. TA-14 has recorded the technical error. Please verify the email and password, then try again.";}
function recordAuthFailure(operation:"login"|"signup",error:unknown):void{const f=normalizeAuthFailure(error);console.error(`[TA-14 account ${operation} failure]`,{code:f.code??"unknown",message:f.message??"No Supabase error message returned.",status:f.status??"unknown"});}

export async function login(formData:FormData):Promise<never>{const next=await resolveNext(formData);let email:string,password:string;try{email=getRequiredField(formData,"email").toLowerCase();password=getRequiredField(formData,"password");}catch{redirectToLogin("error","Enter both your email address and password.",next);}let failure:unknown=null;try{const supabase=await createClient();const result=await supabase.auth.signInWithPassword({email,password});failure=result.error;}catch(error){failure=error;}if(failure){recordAuthFailure("login",failure);redirectToLogin("error","The email address or password was not accepted. If you just created the account, confirm your email before signing in.",next);}redirect(next);}

export async function signup(formData:FormData):Promise<never>{const next=await resolveNext(formData);let email:string,password:string,confirmPassword:string;try{email=getRequiredField(formData,"email").toLowerCase();password=getRequiredField(formData,"password");confirmPassword=getRequiredField(formData,"confirmPassword");}catch{redirectToLogin("error","Complete every account-creation field.",next);}if(password.length<8)redirectToLogin("error","Your password must contain at least eight characters.",next);if(password!==confirmPassword)redirectToLogin("error","The passwords do not match.",next);const requestOrigin=await getRequestOrigin(),confirmationRedirect=new URL("/auth/callback",requestOrigin);confirmationRedirect.searchParams.set("next",next);let signupData:{user:unknown;session:unknown}|null=null,signupFailure:unknown=null;try{const supabase=await createClient();const result=await supabase.auth.signUp({email,password,options:{emailRedirectTo:confirmationRedirect.toString()}});signupData={user:result.data.user,session:result.data.session};signupFailure=result.error;}catch(error){signupFailure=error;}if(signupFailure){recordAuthFailure("signup",signupFailure);redirectToLogin("error",getSignupErrorMessage(signupFailure),next);}if(!signupData?.user){recordAuthFailure("signup",{code:"missing_user",message:"Supabase returned no user and no explicit error."});redirectToLogin("error","The Exchange did not receive a completed account record. Please try again.",next);}if(!signupData.session)redirectToLogin("message","Your Exchange account was created. Open the confirmation email, then return and sign in. Check spam or quarantine folders if the message is delayed.",next);redirect(next);}

export async function logout():Promise<never>{const supabase=await createClient();await supabase.auth.signOut();redirect("/login");}

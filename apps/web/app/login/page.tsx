import Link from "next/link";

import { login, signup } from "./actions";

type LoginPageProperties = { searchParams: Promise<{ error?: string; message?: string; next?: string }> };
function safeNext(value?: string) { if (!value || !value.startsWith("/") || value.startsWith("//") || value.startsWith("/login")) return "/workspace/ai-governance/registry"; return value; }

export default async function LoginPage({ searchParams }: LoginPageProperties) {
  const parameters = await searchParams;
  const next = safeNext(parameters.next);
  const registeringGovernance = next.startsWith("/workspace/ai-governance/registry/register");
  return (
    <main className="account-page"><section className="account-shell">
      <div className="account-introduction"><Link className="brand-link" href="/">TA-14 EXCHANGE PLATFORM</Link><p className="eyebrow">Governed Account Access</p><h1>{registeringGovernance ? "Finish entering the Registry." : "Enter the exchange."}</h1><p className="introduction-copy">{registeringGovernance ? "You already chose to register a governance. Create, recover, or sign in to your Exchange account and TA-14 will return you directly to the registration you requested." : "Create an account to construct governance routes, preserve your work, evaluate admissibility, and maintain continuity between each governed record and its outcome."}</p>{registeringGovernance ? <div className="intent-card"><span className="intent-label">YOUR REQUEST IS PRESERVED</span><strong>Register Governance</strong><p>Account access is an identity boundary, not a rejection.</p></div> : null}<div className="chain">{['Reality','Record','Continuity','Admissibility','Binding','Commit','Execution','Outcome'].map(item=><span key={item}>{item}</span>)}</div><p className="principle">No admissible evidence. No admissible execution.</p></div>
      <div className="account-panel"><div className="panel-heading"><p className="eyebrow">{registeringGovernance ? "Continue Registration" : "Account System"}</p><h2>Sign in, recover, or create an account</h2><p>Your account establishes the identity boundary for saved Exchange records.</p></div>{parameters.error ? <div className="notice notice-error" role="alert">{parameters.error}</div> : null}{parameters.message ? <div className="notice notice-message" role="status">{parameters.message}</div> : null}<div className="autofill-notice"><strong>Browser autofill:</strong> TA-14 does not display or assign stored passwords. A browser or password manager may prefill credentials.</div>
        <div className="form-grid"><form action={login} autoComplete="on" className="account-form"><input type="hidden" name="next" value={next}/><div className="form-heading"><span className="form-number">01</span><div><h3>Existing account</h3><p>Sign in with the identity you already control.</p></div></div><label>Email address<input autoComplete="username" name="email" required type="email"/></label><label>Password<input autoComplete="current-password" minLength={8} name="password" required type="password"/></label><button type="submit">Sign in & continue</button><Link className="recovery-link" href={`/account-recovery?next=${encodeURIComponent(next)}`}>Account exists but password is not working? Recover this identity.</Link></form>
        <form action={signup} autoComplete="off" className={`account-form ${registeringGovernance ? "recommended" : ""}`}><input type="hidden" name="next" value={next}/><div className="form-heading"><span className="form-number">02</span><div><h3>New to the Exchange?</h3><p>Create a new identity only if you do not already have one.</p></div></div>{registeringGovernance ? <div className="recommended-label">NEW REGISTRANTS START HERE</div> : null}<label>Email address<input autoComplete="off" name="email" required type="email"/></label><label>Create password<input autoComplete="new-password" minLength={8} name="password" required type="password"/></label><label>Confirm new password<input autoComplete="new-password" minLength={8} name="confirmPassword" required type="password"/></label><button type="submit">Create account & continue</button></form></div>
        <p className="account-footnote">Creating or recovering an account does not register, approve, certify, authorize, merge, or transfer a governance. Registration remains a separate governed act.</p></div>
    </section><style>{`*{box-sizing:border-box}.account-page{min-height:100vh;padding:48px 24px;background:radial-gradient(circle at top left,rgba(49,92,246,.16),transparent 34%),linear-gradient(135deg,#05070b,#111725);color:#f7f8fb;font-family:Inter,system-ui,sans-serif}.account-shell{width:min(1180px,100%);margin:auto;display:grid;grid-template-columns:minmax(0,.85fr) minmax(0,1.4fr);gap:32px}.account-introduction,.account-panel{border:1px solid rgba(255,255,255,.1);border-radius:28px}.account-introduction{padding:42px;display:flex;flex-direction:column;background:#080c14}.account-panel{padding:42px;background:#f8f9fc;color:#111522}.brand-link{color:#fff;text-decoration:none;font-size:.78rem;font-weight:800;letter-spacing:.16em}.eyebrow{margin:42px 0 14px;color:#8fa8ff;font-size:.74rem;font-weight:800;letter-spacing:.16em;text-transform:uppercase}.panel-heading .eyebrow{margin-top:0;color:#315cf6}h1{font-size:clamp(2.75rem,6vw,5.5rem);line-height:.94;letter-spacing:-.06em}h2{font-size:clamp(2rem,4vw,3.15rem);line-height:1;letter-spacing:-.045em}h3,p{margin-top:0}.introduction-copy,.panel-heading>p{color:#afb7c8;line-height:1.7}.panel-heading>p{color:#626a7a}.intent-card,.autofill-notice,.notice{padding:14px 16px;border-radius:14px;margin-bottom:22px}.intent-card{background:#123326}.intent-label,.recommended-label{color:#51c98a;font-size:.68rem;font-weight:900;letter-spacing:.13em}.chain{margin-top:28px;display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.chain span{padding:12px;border:1px solid #273250;border-radius:999px;text-align:center}.principle{margin:auto 0 0;padding-top:42px}.notice-error{background:#fff0f0;color:#8a1e1e}.notice-message{background:#edfff4;color:#17653a}.autofill-notice{background:#f1f5ff;color:#35415c}.form-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:18px}.account-form{padding:24px;border:1px solid #dde1e9;border-radius:20px;background:#fff}.recommended{border:2px solid #43b879}.form-heading{display:flex;gap:14px;margin-bottom:22px}.form-number{display:grid;width:38px;height:38px;place-items:center;border-radius:50%;background:#111522;color:#fff}.form-heading p{color:#7a8291;font-size:.88rem}label{display:grid;gap:8px;margin-bottom:16px;font-size:.82rem;font-weight:750}input{min-height:48px;padding:0 14px;border:1px solid #cfd5df;border-radius:12px;background:#111522;color:#fff;-webkit-text-fill-color:#fff;caret-color:#fff}input::placeholder{color:#c8ceda;opacity:1}input:-webkit-autofill,input:-webkit-autofill:hover,input:-webkit-autofill:focus{-webkit-text-fill-color:#fff;caret-color:#fff;-webkit-box-shadow:0 0 0 1000px #111522 inset;box-shadow:0 0 0 1000px #111522 inset}input[type=hidden]{display:none}button{width:100%;min-height:50px;border:0;border-radius:12px;background:#315cf6;color:#fff;font:inherit;font-weight:800;cursor:pointer}.recommended button{background:#147a49}.recovery-link{display:block;margin-top:14px;color:#315cf6;font-size:.82rem;font-weight:750;line-height:1.5}.account-footnote{margin-top:24px;color:#747c8b;font-size:.8rem;line-height:1.6}@media(max-width:920px){.account-shell,.form-grid{grid-template-columns:1fr}}`}</style></main>
  );
}

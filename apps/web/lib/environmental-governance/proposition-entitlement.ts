export type EntitlementStanding="ESTABLISHED"|"PARTIAL"|"NOT_ESTABLISHED"|"CONFLICT";
export type EnvironmentalPropositionBoundary={inspectionObject:string;temporalBoundary?:string;spatialBoundary?:string;environmentalMediumOrSystem?:string;conditionOrVariable?:string;thresholdReference?:string};
export type PropositionEntitlement={entitlementId:string;recordFingerprint:string;proposition:string;evidenceRefs:string[];boundary:EnvironmentalPropositionBoundary;standing:EntitlementStanding;limitations:string[];prohibitedExtensions:string[];createdAt:string};
export type EntitlementInput={recordFingerprint:string;proposition:string;inspectionObject:string;evidenceRefs:string[];temporalBoundary?:string;spatialBoundary?:string;environmentalMediumOrSystem?:string;conditionOrVariable?:string;thresholdReference?:string;continuityEstablished:boolean;objectEstablished:boolean;propositionEstablished:boolean;conflictPresent?:boolean;limitations?:string[];prohibitedExtensions?:string[];createdAt?:string};
function stableToken(value:string){let hash=2166136261;for(let i=0;i<value.length;i++){hash^=value.charCodeAt(i);hash=Math.imul(hash,16777619)}return(hash>>>0).toString(16).padStart(8,'0').toUpperCase()}
export function fingerprintEnvironmentalRecord(input:{recordClass:string;sourceText:string;declaredFiles?:{name:string;type:string;size:number;parsed:boolean}[]}){return`TA14-REC-${stableToken(JSON.stringify({recordClass:input.recordClass.trim(),sourceText:input.sourceText,declaredFiles:(input.declaredFiles||[]).map(f=>({name:f.name,type:f.type,size:f.size,parsed:f.parsed})).sort((a,b)=>a.name.localeCompare(b.name))}))}`}
export function establishPropositionEntitlement(input:EntitlementInput):PropositionEntitlement{const limitations=[...(input.limitations||[])],prohibitedExtensions=['Do not expand the determination beyond the declared environmental object.','Do not expand the determination beyond the declared temporal or spatial boundary.','Do not substitute, add, remove, or reinterpret evidence references without establishing a new entitlement.','Do not infer causation, health outcome, or execution authority without separate standing.',...(input.prohibitedExtensions||[])];let standing:EntitlementStanding='ESTABLISHED';if(!input.recordFingerprint.trim()){standing='NOT_ESTABLISHED';limitations.push('The source environmental record identity is not established.')}else if(input.conflictPresent){standing='CONFLICT';limitations.push('Material evidentiary conflict remains unresolved.')}else if(!input.objectEstablished||!input.propositionEstablished){standing='NOT_ESTABLISHED';if(!input.objectEstablished)limitations.push('The environmental object is not sufficiently established.');if(!input.propositionEstablished)limitations.push('The proposition is not sufficiently bounded.')}else if(!input.continuityEstablished){standing='PARTIAL';limitations.push('Continuity is not fully established; entitlement is limited to the evidence intervals actually supported.')}
const identitySeed=JSON.stringify({recordFingerprint:input.recordFingerprint,proposition:input.proposition.trim(),inspectionObject:input.inspectionObject.trim(),evidenceRefs:[...input.evidenceRefs].sort(),temporalBoundary:input.temporalBoundary||'',spatialBoundary:input.spatialBoundary||'',environmentalMediumOrSystem:input.environmentalMediumOrSystem||'',conditionOrVariable:input.conditionOrVariable||'',thresholdReference:input.thresholdReference||''});return{entitlementId:`TA14-ENT-${stableToken(identitySeed)}`,recordFingerprint:input.recordFingerprint,proposition:input.proposition.trim(),evidenceRefs:[...input.evidenceRefs],boundary:{inspectionObject:input.inspectionObject.trim(),temporalBoundary:input.temporalBoundary,spatialBoundary:input.spatialBoundary,environmentalMediumOrSystem:input.environmentalMediumOrSystem,conditionOrVariable:input.conditionOrVariable,thresholdReference:input.thresholdReference},standing,limitations,prohibitedExtensions,createdAt:input.createdAt||new Date().toISOString()}}

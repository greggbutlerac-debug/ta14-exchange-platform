import {NextResponse} from "next/server";
import {randomUUID} from "node:crypto";
import {evaluateVendorPaymentRoute} from "../../../../../../../packages/testing/payment-route-tests";
import {appendRouteVersion,getRoute,signingKey} from "../../../../../../../packages/storage/local-store";
import {parseCorrection} from "../../../../../../../packages/validation/route-requests";
export async function POST(req:Request,{params}:{params:Promise<{rid:string}>}){const correlationId=randomUUID();try{const{rid}=await params;const b=parseCorrection(await req.json());const state=await getRoute(rid);if(!state)throw new Error("Route not found");const r=state.latest;if(r.version!==b.expectedVersion)throw new Error(`VERSION_CONFLICT: expected ${b.expectedVersion}, current ${r.version}.`);const now=new Date().toISOString();const mk=(a:typeof b.procurementAuthority,role:"procurement"|"finance")=>({authorityId:a.authorityId.trim(),role,actorId:r.input.actorId,active:true,effectiveAt:a.effectiveAt,expiresAt:a.expiresAt,boundPaymentHash:r.input.committedPaymentHash!,scopeOrganizationId:r.input.organizationId,scopeSystemId:r.input.systemId});const input={...r.input,routeVersion:r.version+1,beneficiaryVerified:true,authorities:[mk(b.procurementAuthority,"procurement"),mk(b.financeAuthority,"finance")],evidenceBindings:[...(r.input.evidenceBindings??[]),{evidenceId:b.beneficiaryEvidence.evidenceId.trim(),claim:"beneficiary" as const,hash:b.beneficiaryEvidence.hash.toLowerCase(),observedAt:now}],evaluatedAt:now};const key=await signingKey();const receipt=evaluateVendorPaymentRoute(input,key);const out={...r,version:input.routeVersion,input,receipt,createdAt:now};await appendRouteVersion(out,b.expectedVersion,{eventId:randomUUID(),rid,type:"ROUTE_CORRECTED",at:now,version:out.version,details:{decision:receipt.decision,authorityIssuers:[b.procurementAuthority.issuer,b.financeAuthority.issuer],beneficiaryEvidenceSource:b.beneficiaryEvidence.source,previousVersion:b.expectedVersion}});return NextResponse.json({...out,decision:receipt.decision,correlationId});}catch(e){const message=e instanceof Error?e.message:"Unknown error";return NextResponse.json({error:message,correlationId},{status:message.startsWith("VERSION_CONFLICT")?409:400})}}
